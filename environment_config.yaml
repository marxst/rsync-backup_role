attributes:
  metadata:
    # Settings group can be one of "general", "security", "compute", "network",
    # "storage", "logging", "openstack_services" and "other".
    group: 'other'
  base:
    value: '/var/tmp'
    label: 'Backupfolder'
    description: 'Folder where your backups should go'
    weight: 25
    type: "text"
